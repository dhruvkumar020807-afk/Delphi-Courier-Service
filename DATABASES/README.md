# Local databases

The Access databases are intentionally not stored in GitHub because they contain account and application data.

To run the project locally, copy these files from your original PAT folder into this directory:

- USERS DATABASE.mdb
- PACKAGE ORDER.mdb
- REVIEWS.mdb
